function Resource(){
	var name;
	var location;
	var type;
	var level;
	//The amount of resource
	var amount;
	var totalResourceCount;
	
	//read from json file
	//parse it using JSON.parse()
	//stringify it to place into localstorage
	//take it out as needed
	//function to parse it from json.parse form local storage to this array
}